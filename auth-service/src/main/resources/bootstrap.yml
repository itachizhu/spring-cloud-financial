debug: true
spring:
  application:
    name: auth-service
  cloud:
    config:
      uri: http://localhost:8888
      fail-fast: true
      password: admin123
      username: user
#      discovery:
#        enabled: true
#        service-id: config-server

#eureka:
#  client:
#    service-url:
#      defaultZone: http://localhost:4444/eureka/

#server.port=8881
